<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <%= link_to 'Budget-Buddy', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
          <% if user_signed_in?%>
            <li class="nav-item">
              <%= link_to 'Add Expenses', new_expense_path, class: "nav-link active" %>
              <span class="visually-hidden">(current)</span>
            </li>
            <li class="nav-item">
                <%= link_to 'View Expenses', expenses_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to 'Logout', destroy_user_session_path, class: 'nav-link', data: { turbo_method: :delete }%>
            </li>
          <% else %>
            <li class="nav-item">
                <%= link_to 'Sign_Up', new_user_registration_path, class: 'nav-link' %>
              </li>
            <li class="nav-item">
              <%= link_to 'Log-In', new_user_session_path, class: 'nav-link' %>
            </li>
          <% end %> 
      </ul>
    </div>
  </div>
</nav>
